# edu-kit 更新・検証ルール

## 責任分界

| 場所 | 正本として管理するもの | 管理しないもの |
| --- | --- | --- |
| 各資産リポジトリ | 実装、API、CSSクラス、音ID、画像、詳細ガイド | edu-kit全体の選び方 |
| edu-kit | 目的別の選び方、参照順、実在する入口、組み合わせ方 | 全API・全クラスの複製 |
| 教材リポジトリ | 問題データ、文言、正答、達成条件、教材固有UI | 共通部品の再実装 |

edu-kitには最低限の入口と判断基準だけを置き、各資産の詳細仕様を写しません。API変更時の二重更新を減らし、実在確認は自動検証と各正本で行います。

## いつ更新するか

次の場合にedu-kitを確認します。

- 資産リポジトリ名、公開URL、入口ファイル、ガイド名を変更した
- edu-effectsのCSSファイルを追加・削除・改名した
- navi-character-のcatalog構造や画像パスを変更した
- AIが参照順を迷った、または存在しない名前を生成した
- 新しい制作作業を繰り返し行い、独立したSkillが必要になった

APIやクラスの追加だけなら、原則として資産リポジトリ側の実装・ガイドを更新し、edu-kitには詳細を複製しません。

## 検証コマンド

```bash
node scripts/verify-repositories.mjs
```

検証内容:

1. `edu-kit.json`の形式と5資産ID
2. 各リポジトリ、公開カタログ、ガイド、入口の応答
3. `edu-effects/css/`のCSSがedu-effectsのAIガイドに記載されているか
4. navi-character-の`catalog.json`にある全画像が実在するか
5. 8つのSkillが必須見出しを持つか

終了コード`0`が合格、`1`が不合格です。ネットワーク障害も不合格として表示されるため、原因を確認して再実行します。

## 教材での実運用確認

自動検証だけでは、子どもの学習フローは保証できません。代表教材1本で次を通します。

1. edu-kitから該当Skillを選ぶ
2. 利用する共通資産の正本を確認する
3. 開始→出題→正解→不正解→次問→結果→ホーム復帰を確認する
4. スマホ・タブレット横・PCで確認する
5. 迷った場所や推測が発生した場所をedu-kitへ戻す

初回の記録は[`docs/PILOT-KANADOCCHI.md`](docs/PILOT-KANADOCCHI.md)に残します。
