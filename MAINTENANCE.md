# edu-kit 更新・検証ルール

## 責任分界

| 場所 | 正本として管理するもの | 管理しないもの |
| --- | --- | --- |
| 各資産リポジトリ | 実装、API、CSSクラス、音ID、画像、詳細ガイド | edu-kit全体の選び方 |
| edu-kit | 目的別の選び方、参照順、実在する入口、組み合わせ方 | 全API・全クラスの複製 |
| 教材リポジトリ | 問題データ、文言、正答、達成条件、教材固有UI、教材固有の保存 | 共通部品の再実装 |

edu-kitには最低限の入口と判断基準だけを置き、各資産の詳細仕様を写しません。API変更時の二重更新を減らし、実在確認は自動検証と各正本で行います。

## いつ更新するか

次の場合にedu-kitを確認・更新します。

- 資産リポジトリ名、公開URL、入口ファイル、ガイド名を変更した
- edu-effectsのCSSファイルを追加・削除・改名した
- navi-character-のcatalog構造、画像パス、軽量版の構成を変更した
- AIが参照順を迷った、または存在しない名前を生成した
- AIが既存教材の問題、正答、保存、画像リンクを壊した
- 新しい制作作業を繰り返し行い、既存Skillの追記または独立したSkillが必要になった
- シミュレーター、つまずきからの脱出口、報酬の未獲得状態など、複数教材で再利用する基準が生まれた

APIやクラスの追加だけなら、原則として資産リポジトリ側の実装・ガイドを更新し、edu-kitには詳細を複製しません。

## 検証コマンド

~~~bash
node scripts/verify-repositories.mjs
~~~

検証内容:

1. edu-kit.jsonの形式と5資産ID
2. 各リポジトリ、公開カタログ、ガイド、入口の応答
3. edu-effects/css/のCSSがedu-effectsのAIガイドに記載されているか
4. navi-character-のcatalog.jsonにある全画像が実在するか
5. 8つのSkillが必須見出しを持つか

終了コード0が合格、1が不合格です。ネットワーク障害も不合格として表示されるため、原因を確認して再実行します。

## 教材での実運用確認

自動検証だけでは、子どもの学習フローは保証できません。変更した教材で次の確認を通します。

### 内容と進行

- 学習目標、問題、正答、誤答、シミュレーターの現象が正しい
- 開始画面で、何をするか・どの単元か・どのモードかが分かる
- 開始→出題または観察→回答または条件変更→正解→不正解・つまずき→次問または再試行→結果→ホーム復帰を通せる
- 長く止まった場合に、ヒント・解説・答えを見る・再挑戦など、必要な脱出口から再開できる
- 答えを見たことを正答やマスター達成として記録していない

### 保存と成長

- 新規状態、学習途中、リロード後、既存保存データありの状態を確認する
- 保存namespaceが教材固有で、他教材のデータを上書きしない
- 進捗、設定、記録、バッジ、コレクションの獲得状態が意図どおり保持される
- 未獲得の報酬が最初から獲得済みになっていない
- 保存形式を変更した場合、旧形式からの移行と移行後の再読込を確認する

### 画面と操作

- タブレット横向きで、問題・観察対象・主要操作・現在地が基本1画面に収まる
- スマホ縦、タブレット縦、PCでも文字切れ、横スクロール、重なりがない
- 問題文、式、選択肢、結果が絵文字や装飾より読みやすい
- タッチとマウスで操作でき、必要な入力はキーボードでも行える
- 長押し・ドラッグだけを必須にしていない
- シミュレーターは条件変更が現象へ即時反映され、リセットして再試行できる
- 選択中のタブ・モード・単元が見た目で分かる

### 補助資産と失敗耐性

- Web画像は軽量WebPを優先し、実在するURLだけを使用している
- 画像、音、CSS、外部モジュールの読み込み失敗で学習本体が止まらない
- 音なしでも意味が伝わり、prefers-reduced-motionでも操作と結果が分かる
- 白いちらつき、不要な全画面点滅、レイアウトシフト、対象と説明のずれがない
- ブラウザのコンソールに重大なエラーがない
- 外部参照URLに404や推測パスがない

初回の代表教材での確認記録はdocs/PILOT-KANADOCCHI.mdに残します。複数教材で同じ問題が起きた場合は、教材側だけで隠さず、edu-kitまたは正本の資産リポジトリへ改善を戻せるか検討します。
