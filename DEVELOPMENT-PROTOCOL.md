# edu-kit — AI Development Protocol

この文書は、AIが小学校向けWeb教材を新規作成・改修するときの「仕事の進め方」を定義します。

目的は、速く大量に書き換えることではなく、必要な変更だけを安全に行い、既存の学習体験を壊さず、変更後に動作を証明することです。

## 基本原則

- まず調べる。分からないものを推測して実装しない。
- 変更前に、守るものと変えるものを分ける。
- 原因が分からない不具合は、原因を特定する前に全面書き換えしない。
- 変更量を小さく保つ。必要のないリファクタリングを同時に行わない。
- 実装したことではなく、確認できたことを完了条件にする。
- 見た目が直っても、学習・判定・保存・復帰が壊れていれば未完了とする。

## 6段階プロトコル

### PHASE 1 — DISCOVER / 現状把握

コードを編集する前に、対象教材を確認する。

最低限確認するもの：

- 学年、教科、単元、学習目標
- ファイル構成と主要エントリーポイント
- 問題データ、正答、判定、進行
- localStorage等の保存キー・スキーマ・保存タイミング
- 画像、CSS、JS、共通資産の参照先
- 公開画面で実際に使われている導線
- ユーザーが今回変更を求めている箇所

既存教材では、読まずに作り直すことを禁止する。

### PHASE 2 — PROTECT / 保護対象と影響範囲

変更前に次を短く整理する。

- 保護対象：今回壊してはいけないもの
- 変更対象：今回変えるもの
- 変更予定ファイル
- 直接影響する画面・機能
- 間接的に影響する可能性がある画面・機能
- 保存形式への影響
- 共通資産への影響

特に、問題データ、正答、判定、問題数、学習順序、保存スキーマは、依頼に必要でない限り保護対象とする。

### PHASE 3 — DIAGNOSE / 原因特定と実装方針

不具合修正では、症状と原因を分ける。

1. 症状を再現する。
2. どの条件で起きるか確認する。
3. 原因候補をコードと状態から絞る。
4. 原因を説明できてから修正する。

機能追加では、既存の設計に接続できるかを先に確認する。

- 既存Manager・CSS・イベントで実現できないか
- 同じ処理を重複実装しないか
- 学習画面を圧迫しないか
- 既存保存データを壊さないか

複数案がある場合は、変更範囲が狭く、既存挙動を維持できる案を優先する。

### PHASE 4 — IMPLEMENT / 小さく実装

実装中は次を守る。

- 一度に関係ない箇所まで変更しない。
- 既存機能の削除や名前変更を勝手に行わない。
- 共通化済み機能を教材側で再実装しない。
- 画像・API・CSSクラス・音IDは実在確認したものだけ使う。
- 補助資産の失敗で学習本体を止めない。
- UI変更とロジック変更を可能な限り分離する。
- 保存スキーマ変更が必要な場合は旧形式からの移行を用意する。

### PHASE 5 — VERIFY / 変更箇所の検証

実装直後に、今回変更した機能を最短経路で確認する。

例：

- ボタン修正 → タップ・クリック・連打・戻る
- タイマー修正 → 開始・停止・再開・終了・再スタート
- 問題修正 → 正解・不正解・次問・最後の問題
- 保存修正 → 保存・リロード・旧データ・新規データ
- 画像修正 → URL・404・表示サイズ・フォールバック
- レスポンシブ修正 → タブレット横・縦・スマホ・PC

「コード上は正しいはず」で完了しない。

### PHASE 6 — REGRESSION / 回帰確認

変更箇所以外も壊れていないか確認する。

最低限、次の一連の流れを通す。

ホーム → 開始 → モード・単元選択 → 学習 → 正解 → 不正解 → ヒント等の脱出口 → 次へ → 結果 → ホーム → 再スタート

さらに確認する。

- 問題数・正答・学習順序
- 保存とリロード
- タブレット横向きの1画面
- タッチとマウス
- 文字切れ・横スクロール・重なり
- コンソールエラー
- 画像・音が失敗した場合
- reduced motion

最後に skills/final-review/SKILL.md を実行する。

## バグ修正モード

ユーザーが「動かない」「変」「壊れた」「直して」と依頼した場合は、次を優先する。

1. 再現
2. 原因特定
3. 最小修正
4. 同じ原因が他にないか検索
5. 修正箇所の確認
6. 回帰確認

原因が不明なまま、関連ファイル全体を書き換えることを避ける。

## 機能追加モード

ユーザーが機能追加を依頼した場合は、次を確認する。

1. 学習目標に必要か
2. 既存導線のどこへ入れるか
3. 保存が必要か
4. 共通資産で再利用できるか
5. オフライン・GitHub Pagesで成立するか
6. 追加によって既存1画面レイアウトを壊さないか
7. 機能を使わない児童にも邪魔にならないか

## UI改善モード

見た目だけの依頼でも、先に現在の学習導線を確認する。

- 問題・式・観察対象・主要操作を最優先
- 装飾を増やすために学習領域を小さくしない
- 余白を削りすぎて誤タップを増やさない
- タブレット横向きの1画面を基準にする
- スマホでは必要に応じて縦積みにする
- デザイン変更後も判定・進行・保存を確認する

## 「完成」と言ってよい条件

次をすべて満たして初めて完成扱いとする。

- ユーザーの依頼内容が反映されている。
- 保護対象を意図せず変更していない。
- 変更した機能を実際の操作フローで確認した。
- 基本学習フローを最後まで通せる。
- 保存・リロードが必要な教材では正常に保持される。
- 主要端末幅で操作できる。
- 重大なコンソールエラーがない。
- 外部参照が実在する。
- 重大な回帰が0件。

問題が残る場合は「完成」とせず、残っている問題と確認できていない項目を明記する。

## AIの最終報告

作業完了時の報告は、コード量ではなく次を簡潔に示す。

- 何を変更したか
- 原因があった場合は何が原因だったか
- 何を保護したか
- 何を確認したか
- 残課題があるか

「実装しました」だけで終わらず、検証結果まで含める。
